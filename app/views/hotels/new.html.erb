<div class= "new hotel">
	<h2>Add new hotel</h2>

	<div class="">
    <%= bootstrap_form_for(@hotel) do |f| %>

      <%= f.text_field :name %>

      <%= f.text_field :city %>

      <%= f.text_field :street %>

      <%= f.number_field :street_number %>

      <%= f.number_field :zip_code %>

      <%= f.number_field :rating %>

      <%= f.date_field :construction_year %>

      <%= f.date_field :renovation_year %>

      <%= f.submit "Create new hotel", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>

</div>